import discord
from discord.ext import commands
import random
import os

intents = discord.Intents.all()

class Dropdown(discord.ui.Select):
    def __init__(self):

        options = [
            discord.SelectOption(label='Kırmızı', description='En sevdiğin renk kırmızı', emoji='🟥'),
            discord.SelectOption(label='Turuncu', description='En sevdiğin renk turuncu', emoji='🟧'),
            discord.SelectOption(label='Sarı', description='En sevdiğin renk sarı', emoji='🟨'),
            discord.SelectOption(label='Yeşil', description='En sevdiğin renk yeşil', emoji='🟩'),
            discord.SelectOption(label='Mavi', description='En sevdiğin renk mavi', emoji='🟦'),
            discord.SelectOption(label='Mor', description='En sevdiğin renk mor', emoji='🟪'),
            discord.SelectOption(label='Siyah', description='En sevdiğin renk siyah', emoji='⬛'),
        ]


        super().__init__(placeholder='En sevdiğiniz renk...', min_values=1, max_values=1, options=options)

    async def callback(self, interaction: discord.Interaction):
        await interaction.response.send_message(f'Senin en sevdiğin renk {self.values[0]}')


class DropdownView(discord.ui.View):
    def __init__(self):
        super().__init__()

        self.add_item(Dropdown())


class Bot(commands.Bot):
    def __init__(self):
        intents = discord.Intents.default()
        intents.message_content = True

        super().__init__(command_prefix=commands.when_mentioned_or('!'), intents=intents)


bot = Bot()


@bot.command()
async def renk(ctx):

    view = DropdownView()

    await ctx.send('En sevdiğiniz rengi seçin:', view=view)


#Bot Hazır
@bot.event
async def on_ready():
    print(f"{bot.user}'a giriş yaptık.")
    print("Botunuz Online!")



#Sadece benim kullanabilmemi sağlayan kod:
def sadece_ben():
    def predicate(ctx):
        return ctx.message.author.id == 895025358586408981
    return commands.check(predicate)

#Kick Komutu
@bot.command()
@sadece_ben()
@commands.has_permissions(kick_members=True)
async def kick(ctx, user: discord.Member, *, reason="Sebep yok.."):
    try:
            await user.kick(reason=reason)
            embed = discord.Embed(color=discord.Colour.red(), title="", description="")
            embed.add_field(name="Kicked", value=f"""
**{user}** kullanıcısı sunucudan atıldı.
Sebep = **{reason}**
""", inline=True)
            await ctx.send(embed=embed)
    except:
            embed = discord.Embed(color=discord.Colour.red(), title="", description="")
            embed.add_field(name="Kicked", value=f"""
Error
""", inline=True)



@bot.command()
async def merhaba(ctx, to: discord.User = commands.parameter(default=lambda ctx: ctx.author)):
    await ctx.send(f'Merhaba {to.mention} :wave:')


@bot.command()
async def bye(ctx, count_heh = 5):
    await ctx.send("he" * count_heh)


@bot.command()
async def görüşürüz(ctx):
    await ctx.send(":wave:")

@bot.command()
async def davet_linki(ctx):
    await ctx.send("https://discord.gg/7YsV4TsJ")

@bot.command()
async def mesaj_at(ctx, user:discord.Member, *, message=None):
    message = "Naber?"
    embed = discord.Embed(title=message)
    await user.send(embed=embed)

@bot.command()
async def davet_et(ctx, user:discord.Member, *, message=None):
    message = "https://discord.gg/7YsV4TsJ"
    embed = discord.Embed(title=message)
    await user.send("Sunucuya Hilmi tarafından davet edildiniz! Davet linki:")
    await user.send(embed=embed)




#Help Command
@bot.command()
async def yardım(ctx):
    await ctx.send("===Girebileceğiniz=Komutlar===")
    await ctx.send("!merhaba")
    await ctx.send("!heh -sayı-")
    await ctx.send("!görüşürüz")
    await ctx.send("!renk")
    await ctx.send("!mesaj_at @isim")
    await ctx.send("!davet_linki")
    await ctx.send("==Eğlence==")
    await ctx.send("!pythonmeme_at")
    await ctx.send("!oyunmeme_at")


#Örnek:
@bot.command()
async def yumruk_at(ctx, arg):
    await ctx.send(f"{arg} kullanıcısına yumruk attın.") 


@bot.command()
async def pythonmeme_at(ctx):
    liste = os.listdir("memes")
    rastgele_meme = random.choice(liste)
    tam_uzanti = "memes/" + rastgele_meme
    f = open(tam_uzanti, "rb")
    meme = discord.File(f)
    await ctx.send(file=meme)
    await ctx.send(":rofl:")

@bot.command()
async def oyunmeme_at(ctx):
    liste2 = os.listdir("gamememes")
    rastgele_meme2 = random.choice(liste2)
    tam_uzanti2 = "gamememes/" + rastgele_meme2
    f2 = open(tam_uzanti2, "rb")
    meme2 = discord.File(f2)
    await ctx.send(file=meme2)
    await ctx.send(":rofl:")



#Sunucuya biri geldiğinde:
@bot.event
async def on_member_join(member):
    await member.send(f"{member} sunucuya hoşgeldin!:wave:")

#Sunucudan biri çıktığında:
@bot.event
async def on_member_remove(member):
    await member.send(f"{to.mention} sunudan ayrıldı. Gittiğine üzüldük..")



#Token
bot.run("Secret Token Goes Here!")
